- defaults:
    link:
      create: true
      force: true
      relink: true
- clean: ['~']
- link:
    ~/.gitconfig:
    ~/.gitignore_global:
    # ~/nvim/init.vim:
    ~/.tmux.conf:
    ~/.vim:
    ~/.oh-my-zsh/custom: zsh/custom
    ~/.zshrc:
- shell:
  - [git submodule update --init --recursive, Installing submodules]
  - [git clone --depth 1 https://github.com/junegunn/fzf.git ~/.fzf, Installing fzf]

  # - - Making zsh the default shell.
  #   - chsh -s $(which zsh)
  #   - ~/.fzf/install
  # - - Install oh-my-zsh.
  #   - sh -c "$(curl -fsSL https://raw.github.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"
  # - - Installing Node.
  #   - sh -c "$(curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.37.2/install.sh)"
  #   - NVM_DIR="$([ -z "${XDG_CONFIG_HOME-}" ] && printf %s "${HOME}/.nvm" || printf %s "${XDG_CONFIG_HOME}/nvm")"
  #   - export NVM_DIR
  #   - [ -s "$NVM_DIR/nvm.sh" ]
  #   - . "$NVM_DIR/nvm.sh"
  #   - nvm install node
  #   - nvm alias default node
  #   - npm i -g cowsay lolcatjs npm prettier spaceship-prompt

# - brewfile:
#   - Brewfile
